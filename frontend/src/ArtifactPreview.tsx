import type {Artifact} from './Discovery';
export function ArtifactPreview({item}:{item:Artifact}){return item.image?<><img src={item.image} alt={item.title} loading="lazy"/>{item.image_license!=="CC0"&&<small className="image-credit">{item.image_changes}</small>}</>:<div className="text-artifact-preview"><small>TRANSLATED EXCERPT</small><blockquote>{item.passages[0].text}</blockquote><small>{item.institution.split(" — ")[0]} · {item.text_license}<br/>Text-only record · image not included</small></div>;}
